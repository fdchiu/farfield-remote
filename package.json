{
  "name": "codex-monitor-workspace",
  "private": true,
  "packageManager": "pnpm@10.29.3",
  "scripts": {
    "dev": "pnpm --filter @codex-monitor/codex-protocol build && pnpm --filter @codex-monitor/codex-api build && pnpm --parallel --filter @codex-monitor/server --filter @codex-monitor/web dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "sanitize:traces": "node scripts/sanitize-traces.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
